FROM log-parse-ai-base:latest

# Set default environment variables
ENV DB_PATH=/data/logs.duckdb

# Create data directory
RUN mkdir -p /data /models

# Expose API port
EXPOSE 8000

# Set entrypoint
ENTRYPOINT ["uvicorn", "src.services.api.main:app", "--host", "0.0.0.0", "--port", "8000"] 